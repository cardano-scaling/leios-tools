# lie-about-eb-size — rewrite the advertised eb_size on outbound MsgLeiosBlockOffer:
# (eb_size * scale_num / scale_den) + offset. Defaults to identity; override per attack.
Action "lie-about-eb-size" ("lie-about-eb-size", scale_num = 1, scale_den = 1, offset = 0)
