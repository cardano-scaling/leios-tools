# The live-network cluster runner (net-cluster / net-node) lives in the private
# leios-adversarial-tools repository. This public workspace builds the reusable
# networking crates (net-codec, net-core, net-cli).

build:
	cargo build

test:
	cargo test
